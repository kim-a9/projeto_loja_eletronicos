CREATE TABLE cadastro_produto (
	id smallint PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	codigoprod smallint UNIQUE NOT NULL, 
	produto varchar(100) NOT NULL, 
	categoria varchar(50),
	quantidade smallint
);

SELECT * FROM cadastro_produto;